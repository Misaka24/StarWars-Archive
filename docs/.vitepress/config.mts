import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'Misaka Site',
  description: 'Rebuilt from zero',
  themeConfig: {
    nav: [
      { text: '首页', link: '/' }
    ]
  }
})
